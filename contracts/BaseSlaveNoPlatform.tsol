pragma ton-solidity >= 0.61.2;

pragma AbiHeader time;
pragma AbiHeader expire;
pragma AbiHeader pubkey;

import "./BaseSlave.tsol";


abstract contract BaseSlaveNoPlatform is BaseSlave {

    function _onCodeUpgrade(TvmCell data, Version oldVersion, TvmCell params, address caller) internal override {
        onCodeUpgrade(data, oldVersion, params, caller);
    }

    function onCodeUpgrade(TvmCell data, Version oldVersion, TvmCell params, address caller) internal virtual;

}
